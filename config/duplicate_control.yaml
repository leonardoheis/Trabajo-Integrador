# Duplicate detection configuration
# Agent 4 (Duplicate Control) uses this config.
hash_algorithm: sha256

# Action when a duplicate is detected
on_duplicate: reject   # reject | flag_for_review

# Grace period — same hash within this window is always rejected
dedup_window_days: 0   # 0 = no time window, always reject
